# this file is meant to be sourced, not executed

# path of the stored repositories
export GK_REPO_PATH=$(readlink -f $GK_PATH/../repositories)

# interactive shell prompt
export GK_PROMPT="GitKiss > "
export GK_ERR_PROMPT="GitKiss :( > "


# vim:ft=sh
